# script_compilar_latex/suite.yml — suite compilar_latex: Compilador universal de LaTeX (LuaLaTeX + Biber) para los frameworks de escritura, clases y libros; alias `compilar`.
# Contrato: core/suite.schema.yml; core/suites.py validar lo comprueba y generar escribe los índices y README.
id: compilar_latex
objetivo: latex
resumen: Compilador universal de LaTeX (LuaLaTeX + Biber) para los frameworks de escritura, clases y libros; alias `compilar`.
escribe_en:
- archivos
simula_por_defecto: false
comandos:
- main.sh documento.tex
- main.sh --limpiar documento.tex
- main.sh --help
estado: activo
depende_de:
- lualatex
- biber
- core/shell-lib
